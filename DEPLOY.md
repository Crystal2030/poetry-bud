# 诗芽 PoetryBud · 小程序部署方案

> 最后更新：2026-07-25 | 版本 v1.0

---

## 整体概览

| 项目 | 当前状态 | 操作 |
|------|----------|------|
| 小程序代码（43文件） | ✅ 已完成 | 修改配置后即用 |
| 诗词数据 `poems.js`（381KB） | ✅ 已内嵌 | 无需额外操作 |
| 音频 `audio/`（126文件 / 10MB） | ⚠️ 待上传 | 上传至 CDN |
| 背景图 `bg-samples/`（~90MB） | ⚠️ 待上传 | 上传至 CDN |
| TabBar 图标 | ⚠️ 占位圆点 | 替换为正式图标 |

---

## 第一步：注册微信小程序（约 1-3 天）

### 1.1 前往微信公众平台

打开 https://mp.weixin.qq.com/ → 右上角「立即注册」→ 选择「小程序」

### 1.2 填写注册信息

| 字段 | 说明 |
|------|------|
| 邮箱 | 未绑定过微信公众平台的邮箱 |
| 密码 | 设置管理员密码 |
| 主体类型 | 个人或企业（**建议企业**，个人小程序限制较多） |
| 主体信息 | 按提示上传营业执照 / 身份证 |

### 1.3 完成认证

- 企业主体需要微信认证（300元/年），审核约 1-3 个工作日
- 个人主体无需认证，但功能受限（无法使用微信支付、无法开通附近小程序等）

### 1.4 获取 AppID

认证通过后，登录后台 → 左侧菜单「开发」→「开发管理」→「开发设置」→ 复制 **AppID（小程序ID）**

> 记录 AppID：后续步骤会多次用到。

---

## 第二步：设置类目（必须提前做）

提交审核时，小程序必须有正确的服务类目。**教育类目可能需要资质**。

### 2.1 添加类目

后台 →「设置」→「基本设置」→「服务类目」→ 添加

**建议类目**（按优先级）：
1. **教育 > 婴幼儿教育**（无需额外资质）
2. **教育 > 在线教育**（需要 ICP 备案或教委批文）
3. **工具 > 信息查询**（备选，审核容易通过但功能受限）

> ⚠️ 如果选「在线教育」但没有 ICP 备案，审核会被拒。**推荐选「婴幼儿教育」或「教育信息服务」。**

---

## 第三步：下载微信开发者工具

### 3.1 下载

https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

选择 **macOS x64 / ARM64** Stable 版。

### 3.2 安装并登录

打开开发者工具 → 微信扫码登录 → 选择「小程序」项目类型。

---

## 第四步：修改代码配置

### 4.1 替换 AppID

打开 `miniprogram/project.config.json`，找到第 33 行：

```json
"appid": "wx0000000000000000",
```

改成你的真实 AppID：

```json
"appid": "wx你的真实appid",
```

### 4.2 配置 CDN 地址（下面没有CDN资源吗）

非必须 选 CDN !!!! 微信云开发能直接用吗 ??

打开 `miniprogram/app.js`，找到第 3-6 行：

```javascript
const CDN = {
  images: 'https://your-cdn.com/poetrybud/bg-samples/',  // ← 改这里
  audio: 'https://your-cdn.com/poetrybud/audio/'          // ← 改这里
}
```

具体 CDN 地址取决于你第几步选择的方案，后面会说。

### 4.3 （可选）修改项目名称

`project.config.json` 第 34 行：

```json
"projectname": "poetrybud",
```

可改为中文名如 `"projectname": "诗芽PoetryBud"`。

### 4.4 （可选）修改基础库版本

`project.config.json` 第 32 行：

```json
"libVersion": "2.19.4",
```

建议改为当前最新稳定版。开发者工具打开项目后，在「详情」→「本地设置」里可以看到推荐版本。

---

## 第五步：选择资源托管方案

音频（10MB）和背景图（90MB）不能放在小程序代码包中（主包限制 2MB，总包限制 20MB），必须走 CDN / 云存储。

### 方案 A：微信云开发（推荐 ⭐）

**优点**：无需额外注册、免备案、小程序内 `wx.cloud` 原生调用、有免费额度
**缺点**：超出免费额度后按量付费

#### A.1 开通云开发

开发者工具中打开项目 → 顶部「云开发」按钮 → 按提示开通 → 创建环境（名称如 `poetrybud`）

#### A.2 获取环境 ID

云开发控制台 → 左上角「环境名称」旁边就是环境 ID，格式如 `poetrybud-xxxxx`

#### A.3 上传资源文件

在云开发控制台 →「存储」→ 分别创建两个文件夹：
- `bg-samples/` — 上传背景图（含 `themes/` 子文件夹和 7 张 `scene-*.png`）
- `audio/` — 上传 125 首 M4A + `duration-map.json`

#### A.4 配置 CDN 地址

在 `miniprogram/app.js` 中，将 CDN 配置改为：

```javascript
// 使用云存储时，路径为 cloud://环境ID.636c-环境ID-xxxx/文件夹/
const CDN = {
  images: 'cloud://poetrybud-xxxxx.706f-poctrybud-xxxxx/bg-samples/',
  audio: 'cloud://poetrybud-xxxxx.706f-poctrybud-xxxxx/audio/'
}
```

在云开发控制台 → 存储 → 点击任意文件 →「详情」→ 可以看到 `cloud://` 开头的地址，把前缀填进去即可。

#### A.5 预下载音频（可选但推荐）

在 `pages/detail/detail.js` 的 `startAudio()` 方法前加一行：

```javascript
// 音频地址是 cloud:// 开头的需要先换临时链接
getAudioUrl(poem) {
  const cloudPath = CDN.audio + poem.id + '.m4a'
  return new Promise((resolve) => {
    wx.cloud.getTempFileURL({
      fileList: [cloudPath],
      success: res => resolve(res.fileList[0].tempFileURL)
    })
  })
}
```

不过如果音频量不大，直接用 `cloud://` + `wx.cloud.downloadFile` 也行。更简单的方式是直接在云开发控制台 → 存储 → 设置自定义域名，绑定后就可以用 HTTPS 直接访问。

---

### 方案 B：腾讯云 COS（免费额度大）

**优点**：稳定、速度快、与微信生态打通
**缺点**：需要绑定域名并备案

#### B.1 注册腾讯云

https://cloud.tencent.com/ → 注册并实名认证

#### B.2 创建 COS 存储桶

控制台 → 对象存储 COS → 创建存储桶：
- 名称：`poetrybud`
- 地域：按实际选（国内推荐广州/上海）
- 访问权限：**公有读私有写**

#### B.3 上传资源

存储桶内创建目录结构：
```
poetrybud/
  bg-samples/
    themes/    ← 40 张主题图
    scene-*.png ← 7 张场景图
  audio/
    125首 M4A + duration-map.json
```

直接拖拽上传或用 COS 命令行工具上传。

#### B.4 配置 CDN 地址

在 `miniprogram/app.js` 中：

```javascript
const CDN = {
  images: 'https://poetrybud-xxxxx.cos.ap-guangzhou.myqcloud.com/bg-samples/',
  audio: 'https://poetrybud-xxxxx.cos.ap-guangzhou.myqcloud.com/audio/'
}
```

#### B.5 配置小程序合法域名

微信后台 → 开发 → 开发管理 → 开发设置 → **服务器域名** → 添加：

- request 合法域名：`https://poetrybud-xxxxx.cos.ap-guangzhou.myqcloud.com`
- downloadFile 合法域名：`https://poetrybud-xxxxx.cos.ap-guangzhou.myqcloud.com`
- uploadFile 合法域名：`https://poetrybud-xxxxx.cos.ap-guangzhou.myqcloud.com`

> ⚠️ 开发阶段可以在开发者工具中开启「不校验合法域名」，但上传前必须配置。

---

### 方案 C：阿里云 OSS / 七牛云

大同小异，步骤同上：创建 Bucket → 上传文件 → 绑定域名 → 配置 CDN 地址 → 添加小程序合法域名。

---

## 第六步：制作 TabBar 图标

### 6.1 当前占位图标

目前 `miniprogram/static/` 下的 8 个 `tab-*.png` 是纯色圆点占位图，需要替换。

### 6.2 规格要求

| 属性 | 要求 |
|------|------|
| 格式 | PNG（透明背景） |
| 尺寸 | 81 × 81 px（推荐） |
| 大小 | < 40KB / 张 |
| 颜色 | 未选中 #B0A793 · 选中 #4C6B47 |

### 6.3 需要 8 张图标

| 文件名 | 用途 | 建议图标 |
|--------|------|----------|
| `tab-home.png` | 首页（未选中） | 房子 🏠 |
| `tab-home-active.png` | 首页（选中） | 同上，绿色 |
| `tab-grid.png` | 诗库（未选中） | 网格/书 📚 |
| `tab-grid-active.png` | 诗库（选中） | 同上，绿色 |
| `tab-chart.png` | 花园（未选中） | 花朵 🌸 |
| `tab-chart-active.png` | 花园（选中） | 同上，绿色 |
| `tab-user.png` | 我的（未选中） | 人物 👤 |
| `tab-user-active.png` | 我的（选中） | 同上，绿色 |

### 6.4 制作方式

- 找设计师出图（推荐）
- 或用 iconfont.cn 下载 SVG → 导出 81x81 PNG
- 或用 Figma / Sketch 自行绘制

替换后放到 `miniprogram/static/` 目录下，覆盖原文件即可。

---

## 第七步：用开发者工具打开项目

### 7.1 打开项目

1. 打开微信开发者工具
2. 点击「+」或「导入项目」
3. 项目目录：选择 `poetry-app/uni-app/miniprogram/`
4. AppID：填入真实 AppID（见第一步）
5. 项目名称：`诗芽PoetryBud`
6. 点击「确定」

### 7.2 初次编译检查

开发者工具会自动编译。检查控制台是否有红色报错。

**预期状态**：
- 编译成功，模拟器中能看到首页
- 数据正常加载（控制台输出「🌸 诗芽 · 已加载 125 首诗」）
- 四个 Tab 栏切换正常

### 7.3 解决常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 诗词数据为空 | `data/poems.js` 未正确 require | 检查文件路径，确保 `require('./data/poems.js')` 正确 |
| 图片不显示 | CDN 地址未配置 / 域名未添加 | 开发者工具 → 详情 → 本地设置 → 勾选「不校验合法域名」 |
| 音频不能播放 | 同上 | 同上 |
| Tab 图标不显示 | 图标文件缺失 | 确认 `static/tab-*.png` 存在 |

---

## 第八步：预览与调试

### 8.1 真机预览

开发者工具工具栏 →「预览」→ 扫码在手机上预览。

**重点检查**（手机端）：
- [ ] 首页四维探索入口正常显示
- [ ] 诗库搜索和维度切换正常
- [ ] 详情页背景图加载正常
- [ ] 朗读按钮播放音频正常（**注意：开发环境音频可能不走 CDN**）
- [ ] Tab 切换流畅
- [ ] 收藏/已读数据持久化

### 8.2 体验版测试

如果团队有多个成员需要测试：

后台 → 管理 → 成员管理 → 添加「体验成员」→ 开发者工具 →「上传」→ 后台设置「体验版」→ 成员扫码体验。

---

## 第九步：上传代码

### 9.1 最终检查清单

提交前确认：

- [ ] `appid` 已改为真实 ID
- [ ] CDN 地址已配置（云存储或 COS）
- [ ] 音频和背景图已全部上传到 CDN
- [ ] TabBar 图标已替换为正式图标
- [ ] `duration-map.json` 已上传到音频 CDN 目录
- [ ] 服务器域名已在后台配置（requestFile / downloadFile / uploadFile）
- [ ] **无 console.log 调试代码残留**（搜索全局）
- [ ] 在开发者工具中关闭「不校验合法域名」后，真机预览仍正常

### 9.2 上传

开发者工具右上角 →「上传」→ 输入版本号（如 `1.0.0`）和备注（如「首次提交：诗词库125首、四维探索、朗读功能」）→ 确认上传。

---

## 第十步：提交审核

### 10.1 进入审核页面

后台 → 管理 → 版本管理 → 开发版本 → 点击刚上传的版本右侧「提交审核」

### 10.2 填写审核信息

| 字段 | 填写内容 |
|------|----------|
| 配置功能页面 | 按实际情况勾选，建议勾选所有页面 |
| 服务类目 | 确认已正确设置（见第二步） |
| 标签 | 古诗、启蒙、教育、儿童、学习 |
| 小程序简介 | 「诗芽 PoetryBud，专为3-12岁儿童打造的古诗启蒙学习工具，收录125首经典诗词，支持朗读、分类学习、诗词卡片等趣味功能」 |

### 10.3 隐私保护指引

微信要求必须设置隐私协议。

后台 → 设置 → 基本设置 → 服务内容声明 → 用户隐私保护指引 → 填写。

**核心内容**（问你的程序收集什么数据）：
- 用户收藏记录 → 「收藏」
- 阅读计数 → 「阅读记录」

如实勾选即可，不需要过度声明。

### 10.4 提交

确认所有信息无误 → 点击「提交审核」。

---

## 第十一步：等待审核与发布

### 11.1 审核周期

- 通常 **1-7 个工作日**
- 首次提交可能较慢（3-5 天）
- 教育类小程序审核相对宽松

### 11.2 审核被拒怎么办

常见被拒原因及应对：

| 原因 | 解决 |
|------|------|
| 服务类目不匹配 | 换类目（推荐「教育 > 婴幼儿教育」） |
| 功能不完整/页面空白 | 确保所有页面有内容，点击无死链接 |
| 需要补充文娱类资质 | 纯工具+教育类通常不需要。如果需要，可以去掉「朗读」功能走纯信息展示 |
| 隐私协议不完整 | 补齐隐私保护指引 |
| 测试账号 | 提供测试说明 |

每次被拒后修改 → 重新上传 → 重新提交即可，不限制次数。

### 11.3 审核通过后

后台 → 版本管理 → 审核版本 → 点击「发布」→ 确认。

**发布后立即全量上线**，所有微信用户都可以搜索到你的小程序。

### 11.4 发布后检查

- [ ] 微信搜索「诗芽」能搜到
- [ ] 扫码进入后各个页面正常
- [ ] CDN 资源加载正常（音频、背景图）
- [ ] 无白屏或报错

---

## 附录 A：快速修改文件对照表

| 要改什么 | 文件 | 位置 |
|----------|------|------|
| AppID | `project.config.json` | 第 33 行 `"appid"` |
| CDN 图片地址 | `app.js` | 第 4 行 `CDN.images` |
| CDN 音频地址 | `app.js` | 第 5 行 `CDN.audio` |
| TabBar 图标 | `app.json` | 第 22-46 行 `tabBar.list` |
| 导航栏标题 | `app.json` | 第 12 行 `navigationBarTitleText` |
| 项目名称 | `project.config.json` | 第 34 行 `projectname` |

## 附录 B：资源清单

### 需上传到 CDN 的文件

| 路径 | 数量 | 大小 | 说明 |
|------|------|------|------|
| `bg-samples/themes/` | 40 张 PNG | ~74MB | 主题背景图 |
| `bg-samples/scene-*.png` | 7 张 PNG | ~13MB | 场景背景图 |
| `audio/*.m4a` | 125 首 | ~9.8MB | 朗读音频 |
| `audio/duration-map.json` | 1 个 | ~3KB | 音频时长映射 |

### 在小程序代码包内的文件

| 路径 | 大小 | 说明 |
|------|------|------|
| `data/poems.js` | 381KB | 125首诗完整数据 |
| 其余页面/组件代码 | ~50KB | WXML + WXSS + JS |

> 小程序主包总大小预估 **~450KB**，远低于 2MB 限制，安全。

## 附录 C：测试账号信息模板

提交审核时，建议在「版本描述」中附上测试说明：

```
【测试说明】
无需登录，直接浏览即可。
主要功能：
1. 首页 - 每日一诗 + 四维探索入口（朝代/作者/年级/主题）
2. 诗库 - 125首诗词，支持搜索和分类筛选
3. 详情 - 竖排诗词展示 + 朗读 + 收藏
4. 诗卡 - 诗词卡片生成分享
5. 花园 - 学习进度和成就徽章
6. 我的 - 阅读统计和个人设置
```
